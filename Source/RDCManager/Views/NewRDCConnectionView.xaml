<UserControl x:Class="RDCManager.Views.NewRDCConnectionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:RDCManager.Views"
             mc:Ignorable="d">
    <StackPanel>
        <TextBlock Text="Display Name:" FontSize="16" />
        <TextBox x:Name="DisplayName" Height="25" />
        <TextBlock Text="Machine Name:" FontSize="16" />
        <TextBox x:Name="MachineName" Height="25" />
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="30" />
            </Grid.RowDefinitions>
            
            <Button x:Name="Add" Grid.Column="0" Content="Add" />
            <Button x:Name="Cancel" Grid.Column="1" Content="Cancel" />
        </Grid>
    </StackPanel>
</UserControl>
