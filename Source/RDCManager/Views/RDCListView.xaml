<UserControl x:Class="RDCManager.Views.RDCListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:RDCManager.Views"
             mc:Ignorable="d">
    <StackPanel>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="25" />
            </Grid.RowDefinitions>

            <Button x:Name="New" Grid.Column="0" Content="Add" />
            <Button x:Name="Delete" Grid.Column="1" Content="Delete" />
        </Grid>
        <ComboBox ItemsSource="{Binding RDCConnections}" SelectedItem="{Binding SelectedRDCConnection}" Height="30" HorizontalContentAlignment="Center">
            <ComboBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="{Binding DisplayName}" FontSize="16" />
                        <TextBlock Text=" - " FontSize="16" />
                        <TextBlock Text="{Binding MachineName}" FontSize="16" />
                    </StackPanel>
                </DataTemplate>
            </ComboBox.ItemTemplate>
        </ComboBox>
        <Button x:Name="Start" Grid.Row="1" Grid.ColumnSpan="2" Content="Start" Height="30" />
    </StackPanel>
</UserControl>
